# Fase 6 — fine-tuning. Instalar no venv_images:
#   venv_images\Scripts\activate.bat && pip install -r finetune\requirements.txt
peft>=0.13
datasets>=3.0
trl>=0.12
accelerate>=1.0
# transformers e torch já vêm do setup de imagens
